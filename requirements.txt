# —— LLM 提供方 ——
anthropic>=0.40.0          # 公司电脑：Claude Opus 4.8（内网网关）
openai>=1.0.0             # 家庭电脑：DeepSeek（OpenAI 兼容接口）

# —— 检索 ——
chromadb>=0.4.0           # 向量数据库
rank-bm25>=0.2.2          # BM25 关键词检索
# 嵌入模型栈必须 <5：5.x 会把纯文本模型走 AutoProcessor 加载而崩溃
sentence-transformers>=2.2,<5
transformers<5

# —— 文档切片 ——
langchain>=0.1.0
langchain-text-splitters>=0.1.0

# —— 其它 ——
python-dotenv>=1.0.0      # 读 .env
streamlit>=1.28.0         # 前端（开发中）
